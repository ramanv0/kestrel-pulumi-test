name: kestrel-test
runtime: yaml
description: Kestrel Pulumi integration test project
resources:
  pet:
    type: random:RandomPet
    properties:
      prefix: kestrel
  failCmd:
    type: command:local:Command
    properties:
      create: "echo failing-on-purpose && exit 1"
outputs:
  greeting: hello-from-kestrel
  petName: ${pet.id}
